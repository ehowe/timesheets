---
default: &default
  adapter: postgresql

# Local development only
development: &local
  <<: *default
  host: localhost
  username: eugene
  password:
  database: timesheets

test: &test
  <<: *default
  <<: *local
  database: timesheets-test

cypress:
  <<: *test
  database: timesheets-cypress
